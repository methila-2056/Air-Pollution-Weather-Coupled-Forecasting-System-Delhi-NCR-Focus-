name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    name: Backend (ruff + pytest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project and dev dependencies
        run: pip install -e ".[dev]"
      - name: Lint (ruff, scoped to services/API/test code)
        run: python -m ruff check backend/app backend/tests
      - name: Run backend test suite
        run: python -m pytest backend/tests -q --tb=short

  migrations:
    name: Migrations on PostgreSQL
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: aerocast
          POSTGRES_PASSWORD: aerocast_secret_2024
          POSTGRES_DB: aerocast_ncr
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U aerocast -d aerocast_ncr"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://aerocast:aerocast_secret_2024@localhost:5432/aerocast_ncr
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project and dev dependencies
        run: pip install -e ".[dev]"
      - name: Apply Alembic migrations to a fresh PostgreSQL
        run: python -m alembic upgrade head
      - name: Verify migration is at head
        run: python -m alembic current
      - name: Run integration/API tests against PostgreSQL
        run: python -m pytest backend/tests/integration backend/tests/test_api.py -q --tb=short -p no:cacheprovider

  frontend:
    name: Frontend build (tsc + vite)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: npm ci
      - name: Type-check and build
        run: npm run build